════════════════════════════════════════════════════════════════════
EMERGENT — DÜŞÜK KREDİLİ İNCELEME
Repo: MrZekai/pdf-okuyucu   ·   branch: main
════════════════════════════════════════════════════════════════════

ÖNCE: TİP DÜZELTMELERİNİ PUSH ET
  cd ~/Desktop/pdf-okuyucu && git add -A
  git commit -m "tip hatalari duzeltildi" && git push
Push etmezsen Emergent, zaten bulunmuş 4 tip hatasını krediyle
yeniden keşfeder.

════════════════════════════════════════════════════════════════════
EKRANDA NE YAPACAKSIN  (gönderdiğin ekran görüntüsüne göre)
════════════════════════════════════════════════════════════════════

  Sekme          : Mobile App     <- OLDUĞU GİBİ KALSIN
  GitHub çipi    : pdf-okuyucu    <- ZATEN EKLİ, DOĞRU. Kaldırma.
  Platform       : Mobile         <- doğru
  Model          : Auto           <- DEĞİŞTİRME. Premium model seçmek
                                     kredi tüketimini artırır.
  Maxx toggle    : KAPALI         <- KAPALI KALSIN. Açmak maliyeti yükseltir.
  Contest banner : yok say. "Continue" / "View submission" TIKLAMA.

  Kutuda "Build a mobile app:" ön eki duruyor. Bu ön ek ajanı
  "sıfırdan inşa et" moduna çeker. Aşağıdaki prompt ilk satırından
  itibaren bunu iptal ediyor — o yüzden ilk paragrafı silmeyin.

  Sonra tek mesaj olarak gönder. Bölme, parça parça sorma.
  Rapor gelene kadar başka mesaj yazma.

──────────────────────── PROMPT (KOPYALA) ────────────────────────

IGNORE the "Build a mobile app" framing of this input box. Do NOT build,
scaffold, generate or initialise anything. This application ALREADY EXISTS
and is fully implemented in the GitHub repository attached to this message
(MrZekai/pdf-okuyucu, branch main). Your only job in this task is to READ
that repository and produce a report.

ANALYZE ONLY — DO NOT WRITE OR MODIFY ANY CODE IN THIS TASK.

Read AGENTS.md first and obey every rule in it. This is an existing
Expo SDK 57 / React Native / TypeScript PDF reader with a deliberate dense
code style and a 3-language (tr/en/es) i18n layer. My credit budget is very
small, so this must be a single read-only pass.

Scope:
1. Run the project's own cheap checks and report their output verbatim:
      npm run i18n:check
      npm run typecheck
      npx expo-doctor
   Do NOT run a native build. Do NOT run expo prebuild. Do NOT deploy.
   Do NOT publish. The APK is already built for free on GitHub Actions,
   so building here would only waste my credits.

2. Static review, no edits. Group findings as:
   A) BLOCKERS — anything that would fail `expo prebuild` or a Gradle
      Android build, or crash at runtime.
   B) CORRECTNESS RISKS — state/persistence bugs in context/AppContext.tsx
      and lib/storage.ts, file lifecycle bugs in lib/pdfFiles.ts,
      unhandled promise rejections, missing cleanup in hooks.
   C) i18n GAPS — any user-visible string not going through
      constants/i18n.ts, any key that is correct in tr but weak or wrong in
      en/es, any date or number formatting that ignores the active locale.
   D) ANDROID / PLAY READINESS — permissions, the AdMob UMP consent flow in
      hooks/useAdsBootstrap.ts, whether TestIds vs production IDs are wired
      correctly, anything that would fail Play review.
   E) PERFORMANCE — unnecessary re-renders, long lists without
      virtualisation, PdfView remount cost caused by its `key` prop.
   F) IMPROVEMENT OPPORTUNITIES — ranked. For each: what, why it matters,
      exact files touched, and how many files would change. Smallest and
      most reversible first.

3. Hard constraints on your report:
   - Do not propose rewriting the app, switching framework, replacing the
     PDF engine, adding a backend/auth/database/analytics, or upgrading
     packages just because newer versions exist.
   - Do not propose reformatting the code. The dense one-line JSX plus a
     single StyleSheet.create block per file is intentional.
   - Keep kotlinVersion: '2.1.20' in app.config.js. Expo SDK 57 / React Native
     0.86 uses this toolchain, Google Mobile Ads 25.4.0 requires Kotlin 2.1.0,
     and Kotlin 2.3.x crashes while compiling react-native-gesture-handler.
   - Cite file paths and line numbers for every finding.
   - If something is fine, say so in one line. No filler.

4. Output: a single markdown report. No code block longer than 10 lines.
   End with a section titled "CHEAPEST NEXT STEP" naming the ONE task you
   would do first and the exact files it touches.

Then STOP and wait for me to choose a task.

──────────────────────── PROMPT SONU ────────────────────────

════════════════════════════════════════════════════════════════════
RAPOR GELDİKTEN SONRA
════════════════════════════════════════════════════════════════════
Tek işi onaylamak için — yine tek mesaj, dar kapsam:

  Implement ONLY the task named "<...>" from your report.
  Touch only these files: <...>.
  Follow AGENTS.md. Any new user-facing string must be added to all three
  dictionaries in constants/i18n.ts. Then run `npm run check` and show me
  the diff. Do not build, prebuild, deploy or publish.

Sonra "Save to GitHub" ile main dalına push et. GitHub Actions APK'yı
ücretsiz üretir.

════════════════════════════════════════════════════════════════════
KREDİ KURALLARI
════════════════════════════════════════════════════════════════════
  YAPMA : build · prebuild · deploy · publish · Maxx · premium model
  YAPMA : "her şeyi düzelt" gibi geniş istekler — tur sayısı kredi yakar
  YAP   : tek mesaj, dar kapsam, dosya listesi vererek
  YAP   : APK'yı GitHub Actions'tan al (Actions > Expo Android Build >
          Artifacts > pdfokuyucu-expo-apk)

Emergent'in "deployment" özelliği bir WEB barındırma hizmetidir; Android
APK üretmez. Mobil için oraya kredi harcamanın karşılığı yok.
