{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "// baseUrl": "Removed on purpose: deprecated in TypeScript 6 (error TS5101). Since TS 4.4 'paths' resolves relative to this file, so the @/* alias keeps working without it.",
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["**/*.ts", "**/*.tsx", ".expo/types/**/*.ts", "expo-env.d.ts"]
}
